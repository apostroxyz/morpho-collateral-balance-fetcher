{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "typescript",
      "label": "tsc: build - debug.tsconfig.json",
      "tsconfig": "debug.tsconfig.json",
      "problemMatcher": [
        "$tsc"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "options": {
        "cwd": "${workspaceFolder}", // Ensure correct working directory
      }
    }
  ]
}